# Configuration pour Herd avec Secure Site (HTTPS)
# Copier ces variables dans .env quand vous activez le Secure Site

APP_URL=https://yamsoo.test

# Configuration de session pour HTTPS
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax
SANCTUM_STATEFUL_DOMAINS=yamsoo.test,localhost,127.0.0.1

BROADCAST_DRIVER=reverb

REVERB_APP_ID=yamsoo
REVERB_APP_KEY=yamsoo-key-secure-2024
REVERB_APP_SECRET=yamsoo-secret-secure-2024
REVERB_HOST="yamsoo.test"
REVERB_PORT=443
REVERB_SCHEME=https

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

# Instructions :
# 1. Activer "Secure Site" dans Herd pour yamsoo.test
# 2. Copier ces variables dans votre .env
# 3. Recompiler : npm run build
# 4. Démarrer Reverb : php artisan reverb:start
# 5. URL WebSocket : wss://yamsoo.test/app/yamsoo-key-secure-2024
